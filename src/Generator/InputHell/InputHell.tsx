import React from "react";
import TextField from "@mui/material/textfield";

type Props = {};

const InputHell = (props: Props) => {
  return <TextField label="Placeholder" />;
};

export default InputHell;
